{
    "name": "pwc_assigment",
    "version": "1.0.0",
    "scripts": {
      "start": "npm-run-all --parallel start-client start-server",
      "start-client": "cd client && npm install && npm run build && npm start",
      "start-server": "cd server && npm install && npm start"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
      "npm-run-all": "^4.1.5"
    }
  }
  